-# {$ candidates $}


.panel.panel-default
    .panel-heading
        .page-header
            %h1
                エントリーNo.{$ candidates[index].entry_number $} {$ candidates[index].name $}
                %span.small
                    {$ candidates[index].kana $}
    .panel-body
        .row-fluid
            .col-xs-6.col-md-4.col-sm-3..col-lg-2{ng-repeat: "p in photos"}
                %a.thumbnail.thumb{ng-click: "OpenDialog(p.image)"}
                    %img{ng-src: '{$ p.thumb $}' }

    .panel-footer
        .text-center
            %button.btn.btn-primary.btn-lg{type:"button", ng-click:"OpenMail()"}
                {$ candidates[index].name $} に投票する
            %p<
                投票は一人一日一回です。詳しくは
                %a{href: '/vote'}
                    こちら
                をごらんください。


        %table.table.candidate-table
            %tr
                %td
                    出身地
                %td
                    {$ candidates[index].hometown $}
            %tr
                %td
                   　学部
                %td
                    {$ candidates[index].department $}
            %tr
                %td
                    学年
                %td
                    {$ candidates[index].grade $}
            %tr
                %td
                    血液型
                %td
                    {$ candidates[index].blood_type $}
            %tr
                %td
                    趣味
                %td
                    {$ candidates[index].hobby $}
            %tr
                %td
                    応募したキッカケ
                %td
                    {$ candidates[index].motive $}


%script{type: "text/ng-template", id: "DialogTemplate"}
    .ngdialog-img-container
        %img{ng-src: '{$ image_url $}' }
